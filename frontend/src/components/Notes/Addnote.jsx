import React from "react";
import '../Notes/notes.css'

const NavBar = () => {
  return <div>
    <button id="quick-add" onClick={()=>{alert('Feature coming soon!')}}>+Add Note</button>
  </div>;
};

export default NavBar;
